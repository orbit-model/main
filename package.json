{
  "name": "root",
  "description": "A models abstraction for Orbit.",
  "contributors": [
    "Bernhard Halbartschlager <halbart.bernhard+orbit-model@gmail.com>"
  ],
  "keywords": [
    "orbit",
    "orbit.js",
    "orbit-model",
    "models",
    "model"
  ],
  "repository": "https://github.com/bernhard---h/orbit-model",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:watch": "lerna run --stream --no-sort --concurrency=99 build:watch",
    "test": "lerna run test",
    "postinstall": "lerna bootstrap --reject-cycles && cd app && rimraf package-lock.json && npm install"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "chai": "^4.2.0",
    "lerna": "^3.14.1",
    "mocha": "^6.1.4",
    "rimraf": "^2.6.3",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.3333"
  }
}
