{
  "name": "root",
  "description": "A models abstraction for Orbit.",
  "contributors": [
    "Bernhard Halbartschlager <halbart.bernhard+orbit-model@gmail.com>"
  ],
  "keywords": [
    "orbit",
    "orbit.js",
    "orbit-model",
    "models",
    "model"
  ],
  "repository": "https://github.com/bernhard---h/orbit-model",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "@orbit-model/*"
  ],
  "scripts": {
    "install:check": "yarn check && yarn check --integrety && yarn check --verify-tree",
    "build": "lerna run build",
    "build:clean": "yarn run install:check && yarn run build",
    "build:watch": "lerna run --stream --no-sort --concurrency=99 build:watch",
    "test": "lerna run test",
    "postinstall": "lerna bootstrap --reject-cycles && cd app && yarn install --no-lockfile",
    "publish:release": "yarn run build:clean && lerna version --no-push && lerna publish from-git --dist-tag latest --no-git-reset",
    "publish:beta": "yarn run build:clean && lerna version --preid=beta --no-push && lerna publish from-git --preid=beta --dist-tag beta --no-git-reset",
    "publish:canary": "yarn run build:clean && lerna publish --canary --preid=alpha --dist-tag canary --no-git-reset",
    "prettier": "run-p prettier:*:write",
    "prettier:check": "run-p prettier:*:check",
    "prettier:lerna:write": "lerna exec --concurrency=99 -- prettier --config ../../.prettierrc.yaml --list-different --write \"{src,tests}/**/*.{ts,md}\"",
    "prettier:lerna:check": "lerna exec --concurrency=99 -- prettier --config ../../.prettierrc.yaml --check \"{src,tests}/**/*.{ts,md}\"",
    "prettier:app:write": "prettier --config .prettierrc.yaml --list-different --write \"app/src/**/*.{ts,md}\"",
    "prettier:app:check": "prettier --config .prettierrc.yaml --check \"app/src/**/*.{ts,md}\""
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "lerna": "^3.16.4",
    "mocha": "^6.2.0",
    "rimraf": "3.0.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3",
    "prettier": "1.18.2",
    "husky": "^3.0.4",
    "npm-run-all": "^4.1.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run prettier:check",
      "pre-push": "run-p \"yarn run build && yarn run test\" \"cd app && yarn run build && yarn run \""
    }
  }
}
