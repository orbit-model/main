{
  "name": "root",
  "description": "A models abstraction for Orbit.",
  "contributors": [
    "Bernhard Halbartschlager <halbart.bernhard+orbit-model@gmail.com>"
  ],
  "keywords": [
    "orbit",
    "orbit.js",
    "orbit-model",
    "models",
    "model"
  ],
  "repository": "https://github.com/bernhard---h/orbit-model",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:watch": "lerna run build && lerna run --stream --no-sort --concurrency=99 build:watch",
    "postinstall": "lerna bootstrap --reject-cycles"
  },
  "devDependencies": {
    "lerna": "^3.13.0",
    "rimraf": "^2.6.3"
  }
}
