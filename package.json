{
  "name": "root",
  "description": "A models abstraction for Orbit.",
  "contributors": [
    "Bernhard Halbartschlager <halbart.bernhard+orbit-model@gmail.com>"
  ],
  "keywords": [
    "orbit",
    "orbit.js",
    "orbit-model",
    "models",
    "model"
  ],
  "repository": "https://github.com/bernhard---h/orbit-model",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:watch": "lerna run --stream --no-sort --concurrency=99 build:watch",
    "test": "lerna run test",
    "postinstall": "lerna bootstrap --reject-cycles && cd app && npm install --no-package-lock",
    "publish:canary": "npm run build && lerna publish from-git --canary --dist-tag canary --no-git-reset"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "lerna": "^3.16.4",
    "mocha": "^6.2.0",
    "rimraf": "2.6.3",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
